import { Box } from "@chakra-ui/react";
import { SideBar } from "../../Components";

const Admin = () => {
  return (
    <Box className="relative min-h-screen">
        <SideBar />
    </Box>
  );
};

export { Admin };
